<%- include("partials/header"); -%>
    <h1>Home</h1>
    <p><%= content %></p>
    <% for(var i = 0; i < posts.length; i++) { %>
        <h1><%= posts[i].title %></h1>
        <p>
          <%= posts[i].content.substring(0, 100)%>
          <a href="http://localhost:3000/post/<%= posts[i]._id %>">...Read More</a>
        </p>
    <% } %>
<%- include("partials/footer"); -%>
